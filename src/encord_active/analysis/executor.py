
class Executor:
    def __init__(self, AnalysisConfig) -> None:
        pass

    def execute(self):
        pass

class SlowDumbExecutor(Executor):
    def execute(self):
        for data in dataset:
            img = ...
            compute pre-dependencies
            - Embedding stuff

        for data in dataset:
            for metric in metrics:
                run()
